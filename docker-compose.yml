services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ourcelium
      POSTGRES_PASSWORD: ourcelium
      POSTGRES_DB: ourcelium
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
